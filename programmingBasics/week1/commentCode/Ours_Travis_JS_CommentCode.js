// Initializes a variable called likeCount to zero
var likeCount = 0;
// To increase the amount of likes
function increaseLikes()
{
    // Increasing the size of the variable likeCount by one
    likeCount = likeCount + 1;
}